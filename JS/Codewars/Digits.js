function game(matches){
    
    if (matches === -1){
        let x = 0;        
        let res = [x,x+1,x+2,x+3]
    }else if (matches ===1){
    
    }else if (matches ===2){
    
    }else if (matches ===3){

    }
    return res;
    //In first example test case this works because sample code is [1, 2, 3, 4], then
    //first attempt with [1, 2, -1, 4] provides matches == 3 in next input 
  }